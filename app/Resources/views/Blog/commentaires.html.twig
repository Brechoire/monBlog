{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <h1 class="text-center">Liste des commentaire</h1>
            <div class="col-md-6 col-md-offset-3">
                {% for commentaire in commentaires %}
                    {{ commentaire.commentaire }}<br><br>
                {% else %}
                    Il n'y a aucun commentaire
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}